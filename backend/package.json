{
  "name": "retroplayer-backend",
  "version": "1.0.0",
  "description": "Backend API for RetroPlayer — Spotify OAuth, token refresh, API proxy/caching, and user preferences.",
  "main": "src/server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "express-session": "^1.18.0"
  }
}
